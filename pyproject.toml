[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "aegis"
version = "0.1.0"
description = "Zero-trust network sidecar proxy"
readme = "ARCHITECTURE.md"
requires-python = ">=3.9"
dependencies = []

[tool.maturin]
module-name = "aegis._engine"